{
  "name": "z-relay",
  "version": "1.0.0",
  "description": "Zero-knowledge, RAM-only WebSocket relay for the Z messenger. Never stores messages on disk.",
  "main": "server.js",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "ioredis": "^5.4.1",
    "ws": "^8.18.0"
  }
}